apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - services.yaml
configMapGenerator:
  - name: platform-config
    literals:
      - ENV=prod
      - FEATURE_FLAG_REGIONAL_DR=true
      - LOG_LEVEL=info
