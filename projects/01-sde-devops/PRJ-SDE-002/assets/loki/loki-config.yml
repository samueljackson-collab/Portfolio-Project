auth_enabled: false
server:
  http_listen_port: 3100

ingester:
  lifecycler:
    ring:
      kvstore:
        store: "memberlist"

schema_config:
  configs:
  - from: 1970-01-01
    store: boltdb-shipper

storage_config:
  boltdb_shipper:
    active_index_directory: /data/loki/index
    cache_location: /data/loki/cache
  filesystem:
    directory: /data/loki/chunks

compactor:
  retention_enabled: true

limits_config:
  retention_period: 720h

chunk_store_config:
  # Add chunk store configuration here

# Table manager configuration

ruler:
  alertmanager:
    url: "http://alertmanager:9093"