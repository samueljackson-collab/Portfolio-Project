# Example environment variables for monitoring stack
# Copy to .env and replace placeholders with secure values

# Grafana Configuration
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=ChangeThisSecurePassword123!  # Generate via `openssl rand -base64 24`
GF_SERVER_ROOT_URL=http://localhost:3000
GF_INSTALL_PLUGINS=grafana-piechart-panel,grafana-clock-panel

# Prometheus Configuration
PROMETHEUS_RETENTION=15d  # Adjust based on disk capacity; see README for sizing math
PROMETHEUS_STORAGE_PATH=/prometheus
PROMETHEUS_PROXMOX_USER=replace-me
PROMETHEUS_PROXMOX_PASSWORD=replace-me

# Alertmanager Configuration
ALERTMANAGER_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/HERE  # Create via Slack Incoming Webhooks
ALERTMANAGER_SMTP_HOST=smtp.gmail.com:587  # Use app-specific passwords when using Gmail
ALERTMANAGER_SMTP_FROM=alerts@yourdomain.com
ALERTMANAGER_SMTP_TO=monitoring-team@yourdomain.com
ALERTMANAGER_SMTP_USERNAME=your-email@gmail.com
ALERTMANAGER_SMTP_PASSWORD=your-app-specific-password
ALERTMANAGER_PAGERDUTY_KEY=your-pagerduty-routing-key

# General
HOSTNAME=proxmox-monitoring-01
ENVIRONMENT=homelab
TZ=America/Los_Angeles  # Match host timezone for consistent logs

# Guidance:
# - Slack webhook: https://api.slack.com/messaging/webhooks
# - PagerDuty routing key: create via Services -> Integrations
# - Always store secrets in a secure vault (1Password, Vault); never commit real values.
