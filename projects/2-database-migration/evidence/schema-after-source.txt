Unable to capture source schema after migration because Docker is not available in this environment.

Attempted command:
  docker exec -i migration-source-db pg_dump --schema-only --dbname=postgresql://postgres:postgres@localhost:5432/sourcedb
