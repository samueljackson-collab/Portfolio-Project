# Migration Throughput (Rows/sec)

Unable to generate a timing chart because the Docker-based demo stack could not be started in this environment.

Expected data source:
- Migration orchestrator logs for start/end timestamps
- Row count totals from source/target for throughput calculation
