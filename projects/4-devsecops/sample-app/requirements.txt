# Requirements for secure.py
# The vulnerable.py uses basic stdlib + flask for demonstration

# Web framework
flask>=3.1.3

# Security middleware
flask-talisman>=1.1.0
flask-limiter>=4.1.1

# Password hashing
bcrypt>=5.0.0

# SQL with parameterized queries
sqlalchemy>=2.0.49

# Safe XML parsing
defusedxml>=0.7.1

# HTTP client
requests>=2.34.0

# HTML escaping (included with Flask, but explicit)
markupsafe>=3.0.3
