apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-prometheus-exporter.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "custom-prometheus-exporter.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
