route:
  receiver: 'default'
  group_by: ['alertname', 'service']
  group_wait: 30s
  group_interval: 2m
  repeat_interval: 4h
  routes:
    - matchers:
        - severity = "critical"
      receiver: 'critical'
    - matchers:
        - severity = "warning"
      receiver: 'warnings'

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://host.docker.internal:8081/alerts'
        send_resolved: true
  - name: 'warnings'
    webhook_configs:
      - url: 'http://host.docker.internal:8081/alerts/warning'
        send_resolved: true
  - name: 'critical'
    webhook_configs:
      - url: 'http://host.docker.internal:8081/alerts/critical'
        send_resolved: true

inhibit_rules:
  - source_matchers:
      - severity = "critical"
    target_matchers:
      - severity = "warning"
    equal: ['alertname', 'service']
# AlertManager Configuration for P04 Monitoring Stack
# This is a simplified version for local development
# For production, use environment variables for sensitive data

global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default-receiver'
  routes:
    - match:
        severity: critical
      receiver: 'critical-receiver'
      continue: false
    - match:
        severity: warning
      receiver: 'warning-receiver'
      continue: false

receivers:
  - name: 'default-receiver'
    # Default receiver (logs only for local dev)

  - name: 'critical-receiver'
    # Critical alerts - configure email/slack when ready
    # Example webhook for future integration:
    # webhook_configs:
    #   - url: 'http://localhost:5000/alerts'

  - name: 'warning-receiver'
    # Warning alerts - configure slack when ready

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
