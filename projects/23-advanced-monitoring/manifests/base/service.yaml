apiVersion: v1
kind: Service
metadata:
  name: observability-stack
  namespace: monitoring
spec:
  selector:
    app: observability-stack
  ports:
    - name: http
      port: 80
      targetPort: 80
