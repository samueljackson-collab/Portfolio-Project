apiVersion: v1
kind: Service
metadata:
  name: mlflow-registry
  labels:
    app: mlflow
    component: registry
spec:
  ports:
  - port: 5000
    targetPort: 5000
    name: http
  selector:
    app: mlflow
    component: tracking
