# Weekly Wiki Publication Log — 2025-11-14

## Summary
- Generated 25 Wiki.js-ready overview pages for portfolio projects 1-25 under `projects/*/wiki/overview.md` via `scripts/generate_wiki_overviews.py`.
- Embedded architecture, checklist, SLO metric, and screenshot references per coverage matrix requirements.
- Attempted automated push to Wiki.js using `tools/wikijs_push.py` (awaiting valid API endpoint/token for completion).

## Update - Wiki.js Push Automation (Current Run)
- Enhanced `tools/wikijs_push.py` to honor front matter metadata (title, path, tags) and support batch publishing via `--content-root` and `--glob` for the 25 project overview pages.
- Verified 25 overview pages detected under `projects/*/wiki/overview.md` for batch publishing.
- Attempted batch push with `python tools/wikijs_push.py --content-root projects --glob '*/wiki/*.md' --base-path /projects --api-url http://localhost:3000/graphql --api-token "$WIKI_TOKEN"`; connection could not be validated in the sandbox without Wiki.js credentials/endpoints. Re-run in an environment with API access to complete publication and rendering verification.

## Follow-ups
- Re-run `python tools/wikijs_push.py --content-root projects --glob '*/wiki/*.md' --base-path /projects --api-url <wiki-url> --api-token <token>` once Wiki.js API credentials are available.
- Capture fresh UI screenshots to replace shared mockups when individual environments become available.
