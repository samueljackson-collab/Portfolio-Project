# Runbooks

- Queue buildup: inspect Temporal UI, scale workers, pause non-critical workflows
- Failed remediation: review workflow history, requeue task with override, capture RCA
- Secret exposure risk: rotate tokens, purge history if required, audit access logs
