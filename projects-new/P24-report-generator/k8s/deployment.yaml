apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-generator-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-generator-worker
  template:
    metadata:
      labels:
        app: report-generator-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/report-generator:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
