receivers:
  otlp:
    protocols:
      http:
      grpc:
exporters:
  logging:
    loglevel: info
  awsxray:
    region: ${AWS_REGION}
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging, awsxray]
