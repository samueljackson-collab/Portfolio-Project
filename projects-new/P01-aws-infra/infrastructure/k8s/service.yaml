apiVersion: v1
kind: Service
metadata:
  name: aws-infra-service
  labels:
    app: aws-infra
spec:
  selector:
    app: aws-infra
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
