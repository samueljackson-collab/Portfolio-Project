apiVersion: apps/v1
kind: Deployment
metadata:
  name: security-automation-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: security-automation-worker
  template:
    metadata:
      labels:
        app: security-automation-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/security-automation:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
