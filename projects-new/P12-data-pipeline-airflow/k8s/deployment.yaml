apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-pipeline-airflow-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-pipeline-airflow-worker
  template:
    metadata:
      labels:
        app: data-pipeline-airflow-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/data-pipeline-airflow:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
