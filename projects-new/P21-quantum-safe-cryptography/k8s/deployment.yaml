apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantum-safe-cryptography-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantum-safe-cryptography-worker
  template:
    metadata:
      labels:
        app: quantum-safe-cryptography-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/quantum-safe-cryptography:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
