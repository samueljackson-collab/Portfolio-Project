apiVersion: apps/v1
kind: Deployment
metadata:
  name: serverless-api-gateway-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serverless-api-gateway-worker
  template:
    metadata:
      labels:
        app: serverless-api-gateway-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/serverless-api-gateway:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
