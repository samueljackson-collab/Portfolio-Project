apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-website-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-website-worker
  template:
    metadata:
      labels:
        app: portfolio-website-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/portfolio-website:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
