apiVersion: apps/v1
kind: Deployment
metadata:
  name: zero-trust-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zero-trust-worker
  template:
    metadata:
      labels:
        app: zero-trust-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/zero-trust:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
