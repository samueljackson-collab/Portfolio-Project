apiVersion: apps/v1
kind: Deployment
metadata:
  name: advanced-monitoring-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: advanced-monitoring-worker
  template:
    metadata:
      labels:
        app: advanced-monitoring-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/advanced-monitoring:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
