apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: poc-api

resources:
  - base.yaml
  # Secrets should be created separately using kubectl or a secret management tool
  # See SECRETS.md for instructions
