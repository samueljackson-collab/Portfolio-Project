apiVersion: apps/v1
kind: Deployment
metadata:
  name: cost-optimization-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cost-optimization-worker
  template:
    metadata:
      labels:
        app: cost-optimization-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/cost-optimization:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
