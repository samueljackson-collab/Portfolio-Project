apiVersion: batch/v1
kind: Job
metadata:
  name: demo-job
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: demo
          image: local/p15-cost-optimization:latest
          command: ["python", "/app/app.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
      volumes:
        - name: artifacts
          emptyDir: {}
