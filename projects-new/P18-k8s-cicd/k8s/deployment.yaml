apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-cicd-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: k8s-cicd-worker
  template:
    metadata:
      labels:
        app: k8s-cicd-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/k8s-cicd:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
