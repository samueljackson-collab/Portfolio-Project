apiVersion: batch/v1
kind: Job
metadata:
  name: api-contract-tests
  namespace: api-testing
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: runner
          image: api-tests/runner:latest
          env:
            - name: BASE_URL
              value: http://mock-api:8080
