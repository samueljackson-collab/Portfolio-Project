apiVersion: apps/v1
kind: Deployment
metadata:
  name: ha-webapp-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ha-webapp-worker
  template:
    metadata:
      labels:
        app: ha-webapp-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: worker
          image: ghcr.io/example/ha-webapp:latest
          command: ["python", "consumer/worker.py"]
          volumeMounts:
            - name: artifacts
              mountPath: /app/artifacts
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: artifacts
          emptyDir: {}
