# Minimal pinned dependencies observed from repository additions.
# Adjust or pin exact versions as CI reveals.
python-docx>=0.8.11
kubernetes>=26.0.0
requests>=2.28.0
jinja2>=3.1.0
boto3>=1.34.0
psycopg2-binary==2.9.9
PyYAML>=6.0
fastapi>=0.110

# Dev tools (optional, used by CI if present):
black==23.9.1
ruff==0.12.1
pytest>=7.2.0
pytest-cov>=4.1.0
