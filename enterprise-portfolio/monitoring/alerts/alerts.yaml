groups:
  - name: portfolio-alerts
    rules:
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) > 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: High error rate detected
